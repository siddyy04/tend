/// Shared domain enums — exact match to SCHEMA.md.
enum CircleTier {
  innerCircle,
  family,
  friends,
  professional,
  mentors,
  acquaintances,
}

enum MemoryCategory {
  family,
  career,
  health,
  education,
  travel,
  finance,
  goals,
  hobbies,
  preferences,
  promises,
  milestones,
}

enum SourceType { voice, text, photo, share }

enum SensitivityLevel { low, medium, high }

enum DatePrecision { explicit, relative, none }

enum FollowUpStatus { open, done, dismissed }

enum SyncStatus { pending, synced, conflict }
